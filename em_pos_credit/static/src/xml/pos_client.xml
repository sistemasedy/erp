<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">    
    <t
        t-name="ClientListScreen"
        t-inherit="point_of_sale.ClientListScreen"
        t-inherit-mode="extension"
        owl="1"
    >
        
        <xpath expr="//table[hasclass('client-list')]/thead/tr" position="inside">
            <th >
                Deuda
            </th>
            <th >
                Limite
            </th>
            
        </xpath>
        
        
    </t>

    <t t-name="ClientDetailsEdit" t-inherit="point_of_sale.ClientDetailsEdit" t-inherit-mode="extension" owl="1">
        <xpath expr="//div[hasclass('client-details-box.clearfix')]" position="inside">
            <div class="client-detail">
                <span class="label">Tipo</span>
                <select class="detail needsclick" name="property_product_pricelist"
                        t-on-change="captureChange">
                    <t t-foreach="env.pos.pricelists" t-as="pricelist"
                       t-key="pricelist.id">
                        <option t-att-value="pricelist.id"
                                t-att-selected="props.partner.property_product_pricelist ? (pricelist.id === props.partner.property_product_pricelist[0] ? true : false) : false">
                            <t t-esc="pricelist.display_name" />
                        </option>
                    </t>
                </select>
            </div>
        </xpath>
    </t>

    


    <t
        t-extend="ClientLine"
        t-inherit="point_of_sale.ClientLine"
        t-inherit-mode="extension"
        owl="1"
    >
        <xpath expr="//tr" position="inside">
            <td
            >
                <t
                    t-esc='env.pos.format_currency(-props.partner.due_amount)'
                />
            </td>
            <td>
                <t t-esc='env.pos.format_currency(-props.partner.blocking_stage)'/>
            </td>
            
        </xpath>
    </t>






</templates>