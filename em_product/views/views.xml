<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_move_form" model="ir.ui.view">
            <field name="name">account.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <group id="header_right_group" position="replace">
                    <group id="header_right_group">
                        <!-- Invoice date (only invoices / receipts) -->
                        <div class="o_td_label">
                            <label for="invoice_date" string="Invoice Date" style="font-weight:bold;"
                                   attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                            <label for="invoice_date" string="Bill Date" style="font-weight:bold;"
                                   attrs="{'invisible': [('move_type', 'not in', ('in_invoice', 'in_refund', 'in_receipt'))]}"/>
                        </div>
                        <field name="invoice_date" nolabel="1" options="{'datepicker': {'warn_future': true}}"
                               attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}"/>

                        <field name="date" string="Accounting Date"
                               attrs="{
                                    'invisible': [('move_type', 'in', ('out_invoice', 'out_refund', 'out_receipt'))],
                                    'readonly': [('state', '!=', 'draft')],
                               }"/>

                        <!-- Invoice payment terms (only invoices) + due date (only invoices / receipts) -->
                        <label for="invoice_payment_term_id" string="Due Date"
                               attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}"/>
                        <div class="d-flex" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}">
                            <field name="invoice_date_due" force_save="1"
                                   placeholder="Date"
                                   attrs="{'invisible': [('invoice_payment_term_id', '!=', False)]}"/>
                            <span class="o_form_label mx-3 oe_edit_only"
                                  attrs="{'invisible': ['|', ('state', '!=', 'draft'), ('invoice_payment_term_id', '!=', False)]}"> or </span>
                            <field name="invoice_payment_term_id"
                                   placeholder="Terms"/>
                        </div>


                        <label for="journal_id"
                               groups="account.group_account_readonly"
                               invisible="context.get('default_journal_id') and context.get('move_type', 'entry') != 'entry'"/>
                        <div name="journal_div" class="d-flex" >
                            <field name="journal_id"
                                groups="account.group_account_readonly"
                                options="{'no_create': True}"
                                attrs="{'readonly': [('posted_before', '=', True)]}"/>
                            <span class="oe_inline o_form_label mx-3 oe_read_only"
                                groups="base.group_multi_currency"> in </span>
                            <!-- Bigger margin on the left because in edit mode the external link button covers the text -->
                            <span class="oe_inline o_form_label mr-3 ml-5 oe_edit_only"
                                groups="base.group_multi_currency"> in </span>
                            <field name="currency_id"
                                groups="base.group_multi_currency"
                                attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            
                        </div>
                        <label for="validates_journal" string="Comfirmar Diario"/>
                        <div name="journal_div2" class="d-flex" >
                            <field name="validates_journal"/>
                        </div>
                        <label for="validate_journal" string="Verificar El Diario"/>
                        <div name="journal_div2" class="d-flex" >
                            <field name="validate_journal"/>
                        </div>
                    </group>
                </group>

                <xpath expr="//field[@name='ref']" position="after">

                    <field name="fiscal_ref" />
                
                </xpath>


            </field>
        </record>

        





        <record id="view_sale_order_tree_custom" model="ir.ui.view">
            <field name="name">sale.order.tree.custom</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_quotation_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="replace">
                    <tree string="Sales Orders"
                        decoration-info="state == 'draft'"
                        decoration-muted="state == 'cancel'"
                        sample="1">
                        <header>
                            <button name="action_view_invoice"
                                    type="object"
                                    string="Ver facturas"
                                    groups="account.group_account_invoice"/>
                        </header>

                        <field name="name" string="Order Number" decoration-bf="1"/>
                        <field name="partner_id" string="Customer"/>
                        
                        <!-- Date fields -->
                        <field name="date_order" optional="show" string="Order Date"/>
                        <field name="commitment_date" optional="show" string="Commitment Date"/>
                        
                        <!-- Reference fields -->
                        <field name="client_order_ref" optional="hide" string="Customer Reference"/>
                        <field name="origin" optional="hide" string="Source Document"/>
                        
                        <!-- User and company fields -->
                        <field name="user_id" optional="show" string="Salesperson" widget="many2one_avatar_user"/>
                        <field name="company_id" groups="base.group_multi_company" optional="hide"/>
                        
                        <!-- Amount fields -->
                        <field name="amount_untaxed" string="Subtotal" sum="Total" optional="show"/>
                        <field name="amount_tax" string="Taxes" optional="show" sum="Total"/>
                        <field name="amount_total" string="Total" sum="Total" decoration-bf="1" optional="show"/>
                        
                        <!-- Currency fields -->
                        <field name="currency_id" groups="base.group_multi_currency" optional="hide"/>
                        
                        <!-- Activity and status fields -->
                        <field name="activity_ids" widget="list_activity" optional="show"/>
                        
                        <!-- Invoice status -->
                        <field name="invoice_status" 
                            widget="badge"
                            decoration-danger="invoice_status == 'to invoice'"
                            decoration-warning="invoice_status == 'to upsell'"
                            decoration-success="invoice_status == 'invoiced'"
                            optional="show"/>
                        
                        <!-- Order status -->
                        <field name="state" 
                            widget="badge" 
                            decoration-success="state == 'sale'"
                            decoration-info="state == 'draft'"
                            decoration-warning="state == 'sent'"
                            optional="show"/>
                    </tree>
                </xpath>
            </field>
        </record>




        <record id="view_invoice_tree_custom" model="ir.ui.view">
            <field name="name">account.move.tree.custom</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_invoice_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="replace">

                    <tree string="Invoices"
                      js_class="account_tree"
                      decoration-info="state == 'draft'"
                      decoration-muted="state == 'cancel'"
                      sample="1">
                        <header>
                            <button name="action_register_payment" type="object" string="Registrar pago"
                                groups="account.group_account_user"
                                invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt', 'in_invoice', 'in_refund','in_receipt')"/>
                        </header>

                        <field name="partner_vat"/>
                        
                        <field name="invoice_source_email" invisible="1"/>

                        <!-- Campos para proveedor -->
                        <field name="invoice_partner_display_name" invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund','in_receipt')" groups="base.group_user" string="Proveedor" />
                        <field name="fiscal_ref" invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund','in_receipt')" groups="base.group_user"/>
                        
                        <!-- Campos para cliente -->
                        
                        <field name="invoice_partner_display_name" invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')" groups="base.group_user" string="Cliente" />
                        <field name="name" decoration-bf="1" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))]}"/>
                        
                        <field name="company_id" groups="base.group_multi_company" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))]}"/>
                        
                        
                        <!-- Campos invisibles -->
                        
                        <field name="invoice_date" optional="show" invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund','in_receipt')" string="Bill Date"/>
                        <field name="invoice_date" optional="show" invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')" string="Invoice Date"/>
                        <field name="date" optional="hide" invisible="context.get('default_move_type') in ('out_invoice', 'out_refund', 'out_receipt')" string="Accounting Date"/>
                        <field name="invoice_date_due" widget="remaining_days" optional="show" attrs="{'invisible': [['payment_state', 'in', ('paid', 'in_payment', 'reversed')]]}"/>
                        <field name="invoice_origin" optional="hide" string="Source Document"/>
                        <field name="payment_reference" optional="hide" invisible="context.get('default_move_type') in ('out_invoice', 'out_refund','out_receipt')"/>
                        <field name="ref" optional="hide"/>
                        <field name="invoice_user_id" optional="hide" invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')" string="Salesperson" widget="many2one_avatar_user"/>
                        <field name="activity_ids" widget="list_activity" optional="show"/>
                        <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="hide"/>
                        <field name="amount_untaxed_signed" string="Sut Total"  sum="Total" optional="show"/>
                        <field name="amount_tax_signed" string="Itbis" optional="show" sum="Total"/>
                        <field name="amount_total_signed" string="Total" sum="Total" decoration-bf="1" optional="show"/>
                        <field name="amount_total_in_currency_signed" string="Total in Currency" groups="base.group_multi_currency"  optional="show"/>
                        <field name="amount_residual_signed" string="Amount Due" sum="Amount Due" optional="hide"/>
                        <field name="currency_id" groups="base.group_multi_currency" optional="hide"/>
                        <field name="company_currency_id" invisible="1"/>
                        <field name="to_check" optional="hide" widget="boolean_toggle"/>
                        <field name="payment_state"
                               widget="badge"
                               decoration-danger="payment_state == 'not_paid'"
                               decoration-warning="payment_state in ('partial', 'in_payment')"
                               decoration-success="payment_state in ('paid', 'reversed')"
                               attrs="{'invisible': [('payment_state', 'in', ('invoicing_legacy'))]}"
                               optional="show"/>
                        <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-info="state == 'draft'" optional="show"/>
                        <field name="move_type" invisible="context.get('default_move_type', True)"/>
                  </tree>


                </xpath>
            </field>
        </record>


        <record id="view_invoice_filter_custom" model="ir.ui.view">
            <field name="name">account.move.select.custom</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_account_invoice_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="replace">
                    

                   <search string="Search Invoice">
                        <field name="name" string="Invoice"
                               filter_domain="[
                                    '|', '|' , '|', '|',
                                    ('name', 'ilike', self), ('invoice_origin', 'ilike', self),
                                    ('ref', 'ilike', self), ('payment_reference', 'ilike', self),
                                    ('partner_id', 'child_of', self)]"/>
                        <field name="journal_id"/>
                        <field name="partner_id" operator="child_of"/>
                        <field name="invoice_user_id" string="Salesperson" domain="[('share', '=', False)]"/>
                        <field name="date" string="Period"/>
                        <filter domain="[('invoice_user_id', '=', uid)]" name="myinvoices" help="My Invoices"/>
                        <separator/>
                        <filter string="B15" name="guv" domain="[('l10n_do_fiscal_number', 'ilike', 'B15')]"/>
                        <filter string="B01" name="b01" domain="[('l10n_do_fiscal_number', 'ilike', 'B01')]"/>
                        <separator/>
                        <filter name="draft" string="Borrador" domain="[('state','=','draft')]"/>

                        <filter name="posted" string="Publicado" domain="[('state', '=', 'posted')]"/>
                        <filter name="cancel" string="Cancelado" domain="[('state', '=', 'cancel')]"/>
                        <separator/>
                        <filter string="Para revisar" name="to_check" domain="[('to_check', '=', True)]"/>
                        <separator/>
                        <!-- not_paid & partial -->
                        <filter name="open" string="No pagado" domain="[('state', '=', 'posted'), ('payment_state', 'in', ('not_paid', 'partial'))]"/>
                        <!-- in_payment & paid -->
                        <filter name="closed" string="Pagado" domain="[('state', '=', 'posted'), ('payment_state', 'in', ('in_payment', 'paid'))]"/>
                        <!-- overdue -->
                        <filter name="late" string="Atrasadas" domain="[
                            ('invoice_date_due', '&lt;', time.strftime('%%Y-%%m-%%d')),
                            ('state', '=', 'posted'),
                            ('payment_state', 'in', ('not_paid', 'partial'))
                        ]" help="Overdue invoices, maturity date passed"/>
                        <separator/>
                        <filter name="invoice_date" string="Fecha de factura" date="invoice_date"/>
                        <filter name="date" invisible="context.get('default_move_type') in ('out_invoice', 'out_refund', 'out_receipt')" string="Accounting Date" date="date"/>
                        <filter name="due_date" string="Fecha de vencimiento" date="invoice_date_due"/>
                        <separator/>
                        <filter invisible="1" string="Late Activities" name="activities_overdue"
                            domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                            help="Show all records which has next action date is before today"/>
                        <filter invisible="1" string="Today Activities" name="activities_today"
                            domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                        <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                            domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                        <group expand="0" string="Group By">
                            <filter string="Salesperson" name="salesperson" context="{'group_by':'invoice_user_id'}"/>
                            <filter name="status" string="Status" context="{'group_by':'state'}"/>
                            <separator/>
                            <filter string="Invoice Date" name="invoicedate" context="{'group_by': 'invoice_date'}"/>
                            <filter string="Due Date" name="duedate" context="{'group_by': 'invoice_date_due'}"/>
                        </group>
                   </search>


                </xpath>
            </field>
        </record>




        <record id="view_pos_payment_search5" model="ir.ui.view">
            <field name="name">pos.payment.search.view</field>
            <field name="model">pos.payment</field>
            <field name="inherit_id" ref="point_of_sale.view_pos_payment_search"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="replace">
                    

                   <search string="Search Invoice">

                        <field name="name"/>
                        <field name="amount"/>
                        <field name="pos_order_id" />
                        <filter name="banco" string="Targeta" domain="[('payment_method_id', '=', 'Banco')]"/>
                        <filter name="superate" string="Superate" domain="[('payment_method_id', '=', 'Targeta Superate')]"/>
                        <separator/>
                        <filter name="payment_date" string="Fecha" date="payment_date"/>
                        <group expand="0" string="Group By">
                            <filter name="group_by_payment_method" string="Payment Method" domain="[]"  context="{'group_by':'payment_method_id'}"/>
                            <filter name="group_by_session" string="Session" domain="[]"  context="{'group_by':'session_id'}"/>
                        </group>
                        
                   </search>


                </xpath>
            </field>
            
        </record>


        <record id="view_purchase_order_tree_fiscal" model="ir.ui.view">
            <field name="name">purchase.order.tree.fiscal</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="attributes">
                    <attribute name="editable">bottom</attribute>
                </xpath>
                <xpath expr="//field[@name='amount_total']" position="after">
                    <field name="monto"/>
                    <field name="itbis"/>
                    <field name="subtotal"/>
                </xpath>
            </field>
        </record>


        <record id="action_purchase_order_fiscal" model="ir.actions.act_window">
            <field name="name">Compras Fiscales</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">purchase.order</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_purchase_order_tree_fiscal"/>
        </record>

        <menuitem id="menu_purchase_fiscal" name="Compras Fiscales" parent="purchase.menu_purchase_root" action="action_purchase_order_fiscal"/>


        



    

    </data>
</odoo>
